{
  "config": {
    "step": {
      "user": {
        "title": "Location Share",
        "description": "Pick the people to track. The base URL is used to build share links, so it must be the address that people you share with can reach.",
        "data": {
          "entities": "People / device trackers",
          "base_url": "Public base URL of this Home Assistant",
          "home_zone": "Home zone",
          "default_share_minutes": "Default share duration (minutes)",
          "default_speed_kmh": "Fallback speed for ETA (km/h)"
        }
      }
    },
    "abort": {
      "single_instance_allowed": "Location Share is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Location Share options",
        "data": {
          "entities": "People / device trackers",
          "base_url": "Public base URL of this Home Assistant",
          "home_zone": "Home zone",
          "default_share_minutes": "Default share duration (minutes)",
          "default_speed_kmh": "Fallback speed for ETA (km/h)"
        }
      }
    }
  }
}